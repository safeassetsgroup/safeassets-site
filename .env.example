# Copy to .env.local for local development

# Resend
RESEND_API_KEY=your_resend_api_key
EMAIL_FROM=SafeAssets <no-reply@safeassets.group>
# Optional override; defaults to sales@safeassets.group when unset
# EMAIL_TO=sales@safeassets.group

# reCAPTCHA (optional; app skips verification if not set)
# Obtain site key and secret from Google reCAPTCHA v3
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=
RECAPTCHA_SECRET_KEY=
